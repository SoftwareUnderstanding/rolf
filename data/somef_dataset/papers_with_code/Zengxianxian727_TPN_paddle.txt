# TPN_paddle

This project is an unoffical code of TPN (cvpr2020) https://arxiv.org/pdf/2004.03548.pdf with Paddlepaddle 1.8.x

The origin project of TPN is in https://github.com/decisionforce/TPN with pytorch and mmaction

## Training (have trained with a small subset of k400)
cd code && python train_new.py


## Test (using the pretrained model from TPN).
cd code && python eval.py

